defmodule EventSource.Accumulator do
  @moduledoc """
  The Query module listens for events in the store and builds a view of the
  current state of the system.

  Think of it like the reducer in a redux implementation.
  """
end
