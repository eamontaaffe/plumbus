defmodule EventSource.Action do
  @moduledoc """
  The `Action` module is the API for dispatching events.

  It's primary interface with the store is the `:dispatch` call. Think of this
  module like the action interface of Redux.
  """
end
